# 백엔드 환경변수 예시. 이 파일을 복사해 .env 로 만들고 실제 값을 채운다.
# 실제 키가 든 .env 는 절대 커밋하지 않는다.

# Google Gemini API 키. 리라이터가 쓴다. 필수.
GEMINI_API_KEY=your-gemini-api-key-here

# 쓸 모델 이름. 없으면 코드 기본값(gemini-flash-latest) 사용. 선택.
GEMINI_MODEL=gemini-flash-latest
