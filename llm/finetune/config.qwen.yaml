# 리라이터 LoRA 파인튜닝 — Qwen2.5 대안 config.
#
# 왜 대안이 필요한가(2026-07-08 확인):
#   1. EXAONE 3.5 라이선스는 연구용 — 상용 서비스는 LG AI Research와 별도 계약 필요.
#      SightSpace 상용화 관점에서 Qwen2.5-7B-Instruct(Apache-2.0)가 안전한 베이스.
#   2. EXAONE은 커스텀 아키텍처라 Unsloth 미지원 가능성 높음(공식 지원 목록에 없음).
#      Qwen2.5는 Unsloth 네이티브 지원 → train_lora.py(빠른 경로)로 학습 가능.
#
# 실행:  python train_lora.py --config config.qwen.yaml     (Unsloth)
#   또는 python train_lora_hf.py --config config.qwen.yaml  (HF 엔진도 물론 동작)
# 베이스 선정은 검토 창 결정 사항 — 이 파일은 "결정 즉시 돌릴 수 있게" 준비해 둔 것.
# 학습 데이터·시스템 프롬프트는 config.yaml(EXAONE)과 완전히 동일 — 순수 베이스 비교 실험.

base_model: "Qwen/Qwen2.5-7B-Instruct"
max_seq_length: 2048
load_in_4bit: true

# Qwen2.5 채팅 템플릿 마커 (ChatML). train_lora*.py 가 실물 검증한다.
instruction_part: "<|im_start|>user\n"
response_part: "<|im_start|>assistant\n"

lora_r: 16
lora_alpha: 16
lora_dropout: 0.0
lora_target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - up_proj
  - down_proj

train_file: "data/train_chat.jsonl"
eval_file: ""

num_train_epochs: 3
per_device_train_batch_size: 2
gradient_accumulation_steps: 4
learning_rate: 2.0e-4
warmup_ratio: 0.05
weight_decay: 0.01
lr_scheduler_type: "cosine"
logging_steps: 5
save_steps: 50
seed: 3407

output_dir: "outputs/blindguide-rewriter-qwen-lora"
save_merged_16bit: true
save_gguf: true
gguf_quant: "q4_k_m"
